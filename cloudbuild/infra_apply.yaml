steps:
  - name: 'hashicorp/terraform'
    args: ['init']
    dir: 'terraform/enviroments/${_ENV}'

  - name: 'hashicorp/terraform'
    args: ['apply', '-auto-approve']
    dir: 'terraform/enviroments/${_ENV}'

options:
  logging: CLOUD_LOGGING_ONLY