steps:
  # 1. Terraformの初期化
  - name: 'hashicorp/terraform'
    args: ['init']
    dir: 'terraform'

  - name: 'hashicorp/terraform'
    args: ['plan']
    dir: 'terraform'

  - name: 'hashicorp/terraform'
    args: ['apply', '-auto-approve']
    dir: 'terraform'

options:
  logging: CLOUD_LOGGING_ONLY